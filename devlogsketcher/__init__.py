"""DevlogSketcher — outline devlog/social posts from a repo's git history.

It reviews recent changes and proposes *outlined* post ideas into a per-project
database. It never writes the actual post.
"""

__version__ = "0.1.0"
